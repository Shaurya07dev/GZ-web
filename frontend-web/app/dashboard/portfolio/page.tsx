import { PortfolioBoard } from "@/features/dashboard/portfolio-board";

export const metadata = {
  title: "Portfolio | GalleryZone Artist Dashboard",
};

export default function ArtistPortfolioPage() {
  return <PortfolioBoard />;
}
