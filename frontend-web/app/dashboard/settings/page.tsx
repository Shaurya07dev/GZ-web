import { ArtistSettingsView } from "@/features/dashboard/artist-settings-view";

export const metadata = {
  title: "Settings | GalleryZone Artist Dashboard",
};

export default function ArtistSettingsPage() {
  return <ArtistSettingsView />;
}
