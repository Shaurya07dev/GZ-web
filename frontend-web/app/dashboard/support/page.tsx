import { SupportView } from "@/features/dashboard/support-view";

export const metadata = {
  title: "Support | GalleryZone Artist Dashboard",
};

export default function ArtistSupportPage() {
  return <SupportView />;
}
