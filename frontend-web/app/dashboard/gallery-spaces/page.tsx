import { GallerySpacesTable } from "@/features/dashboard/gallery-spaces-table";

export const metadata = {
  title: "Gallery Spaces | GalleryZone Artist Dashboard",
};

export default function ArtistGallerySpacesPage() {
  return <GallerySpacesTable />;
}
